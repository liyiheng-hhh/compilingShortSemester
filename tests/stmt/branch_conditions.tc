int main() {
    int i = 0;
    int sum = 0;
    int limit = 6;
    while (!(i >= limit) && (i != 4 || sum < 6)) {
        i = i + 1;
        if (!(i <= 2) && (i > 4 || i == 3)) {
            sum = sum + 10;
        } else {
            sum = sum + i;
        }
    }
    return sum;
}
