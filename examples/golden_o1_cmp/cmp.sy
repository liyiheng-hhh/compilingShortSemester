int main() {
  int a;
  int x;
  a = 5;
  if (a > 0) {
    x = 1;
  } else {
    x = 0;
  }
  putint(x);
  putch(10);
  if (0 < a) {
    x = 1;
  } else {
    x = 0;
  }
  putint(x);
  putch(10);
  if (a == 5) {
    x = 1;
  } else {
    x = 0;
  }
  putint(x);
  putch(10);
  return 0;
}
